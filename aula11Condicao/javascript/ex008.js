let velocidade = 50.5;
console.log(`A velovidade atual é de ${velocidade} Km/h`);

/* Condição simples */
if(velocidade > 60) {
    console.log(`Sua velocidade é de ${velocidade} Km/h está acima do limite, você será multado`);
}

console.log(`Dirija com segurança, use o cinto`);
